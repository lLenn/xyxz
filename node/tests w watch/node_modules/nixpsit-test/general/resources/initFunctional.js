const webdriver = require("nixpsit-test").testing.getWebdriver();

beforeEach(function() {
	webdriver.executeScript(function() {
		var parent = $("#tests_container").parent(); 
		$("#tests_container").remove();
		parent.append($("<div id='tests_container'>"));
	});
});